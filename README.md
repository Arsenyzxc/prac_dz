
# Учебный конфигурационный язык → XML
## Вариант 25
Этот проект содержит транслятор учебного конфигурационного языка в формат **XML**.  
Программа читает входной текст, выполняет синтаксический разбор, вычисляет константы и выражения в постфиксной форме, а затем формирует итоговый XML-файл.

##  Возможности

- Поддержка:
  - чисел  
  - словарей `begin ... end`
  - комментариев:  
    - однострочных `||`
    - многострочных `=begin ... =cut`
  - константных выражений в постфиксной форме `$ ... $`
  - операций `+ - * / min`
- Проверка синтаксических и семантических ошибок  
- Генерация корректного XML-документа

##  Использование

### Запуск

```bash
python dz_xml.py -i config.txt -o result.xml
```

Где:

- `-i config.txt` — входной файл с конфигурацией  
- `-o result.xml` — выходной XML-файл  

Если аргумент `-i` не указан, программа читает данные из `stdin`.

### Пример входного файла

```
|| комментарий
pi: 3.14;
a: 10;
b: 20;
c: $ a b + $;

tbl: begin
    x := 1;
    y := $ b c + $;
end;
```

### Пример результата

```xml
<config>
    <number name="pi">3.14</number>
    <number name="a">10.0</number>
    <number name="b">20.0</number>
    <number name="c">30.0</number>
    <dict name="tbl">
        <number name="x">1.0</number>
        <number name="y">50.0</number>
    </dict>
</config>
```

##  Структура проекта

```
.
├── dz_xml.py       # основной транслятор
├── config.txt      # пример входного файла
└── README.md       # документация проекта
```

## 📝 Лицензия

Свободное использование в учебных целях.
